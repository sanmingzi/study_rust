# Cargo

```sh
cargo new hello_cargo
cd hello_cargo

# cargo build
# ./target/debug/hello_cargo

cargo run # build and run app

cargo check

# cargo build --release
# ./target/release/hello_cargo

cargo run --release
```
